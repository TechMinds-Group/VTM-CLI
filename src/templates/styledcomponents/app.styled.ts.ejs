import styled, { keyframes } from 'styled-components';

export const AppContainer = styled.div`
  width: 97vw;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
 padding: 10px;
`;

export const ContainerLogos = styled.div`
  max-width: 1280px;
  margin: 0 auto;
  padding: 2rem;
  text-align: center;
  flex: 1;

  a:nth-child(4) {
    display: block;
  }

  .logo {
    height: 6em;
    padding: 1.5em;
    will-change: filter;
    transition: filter 300ms;


    &.vite:hover {
      filter: drop-shadow(0 0 2em #ffeb3b);
    }

    &.techminds:hover {
      filter: drop-shadow(0 0 2em #673ab7);
    }

    &.react:hover {
      filter: drop-shadow(0 0 2em #03a9f4);
    }

    &.styledcomponents:hover {
      filter: drop-shadow(0 0 2em #ec407a);
    }
  }

  @keyframes logo-spin {
    from {
      transform: rotate(0deg);
    }
    export default Container;
    to {
      transform: rotate(360deg);
    }
  }

  @media (prefers-reduced-motion: no-preference) {
    a:nth-of-type(3) .logo {
      animation: ${keyframes`
        from {
          transform: rotate(0deg);
        }
        to {
          transform: rotate(360deg);
        }
      `} infinite 20s linear;
    }
  }
`;

export const AppCardStyled = styled.div`
  padding: 2em;
`;

export const AppReadTheDocStyled = styled.p`
  color: #888;
`;